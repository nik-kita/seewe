---
kind: log
date: 2026-06-13
---

# Slice 4d done: CDN cache headers (slice 4 complete)

Added `api/spa_subserver/page_cache.ts` + wired it into `page_subserver`.

- `cache_headers(page, s_maxage)` -> `Deno-CDN-Cache-Control: public,
  s-maxage=<n>` + `Deno-Cache-Tag: page-<id>,user-<id>` (the tags slice 5 will
  purge on write).
- `s-maxage` from `CACHE_S_MAXAGE` env, default `DEFAULT_S_MAXAGE = 31_536_000`
  (1y). Long on purpose: invalidation is by tag on write (global, seconds), not
  by TTL ([[011.decision]]). `resolve_s_maxage` rejects junk/negative/float ->
  default. `S_MAXAGE` resolved once at startup.
- `respond()`: only the cacheable page CONTENT (`artifact` / `site`) gets the
  headers via `with_cache`; redirects and not-found do not. Used
  `Deno-CDN-Cache-Control` (not `Cache-Control`) so it stays a CDN-only directive.

Verified: 4 hermetic unit tests (env default/override/junk, tag composition);
integration check shows the headers on /CacheGuy (s-maxage=31536000, page-/user-
tags) and ABSENT on the case-redirect + 404. `deno check` + `deno task test`
(18) clean.

Slice 4 (public serving) is COMPLETE: 4a render machine, 4b user nik lc/display,
4c handler, 4d CDN cache. Remaining in the rebrand: slice 5 invalidate-on-write
(purge page-/user- tags via cache.localhost), 6 routers (/v1/page), 7 cutover
(mount page_subserver, post-pivot schema photo, hand off to paused migration).
