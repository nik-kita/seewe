---
kind: log
date: 2026-06-13
---

# Slice 2 done: reserved-username guard

Added:
- `api/dev/gen_reserved_usernames.ts` — generator. Union of: app route slugs read
  locally from `src/pages` (authoritative, stays in sync), a curated infra/role/
  brand/future-route set, and best-effort fetch of The Big Username Blacklist +
  LDNOOBW(en). Writes a sorted `ReadonlySet` literal.
- `api/utils/reserved_usernames.gen.ts` — generated, committed, 962 entries
  (7 slugs + curated + 542 blacklist + 404 ldnoobw, deduped).
- `api/utils/is_reserved_username.ts` — exact match on the normalized (trim +
  lowercase) form. Deliberately NOT substring/leet (avoids the Scunthorpe false
  positive).
- Enforcement in `users_service.add_nik` + `update_nik`, before the uniqueness
  check, returning "This nik is reserved".

Verified: admin/ADMIN/" Admin " (case+trim), dashboard/md-cv (slugs), seewe
(brand), support (list) -> reserved; alice/nikkita -> free. `deno check mod.ts`
clean.

Note: normalization here (trim+lowercase) is the SAME form the link resolver and
the lowercased `username` index will use ([[010.decision]] display/index split) —
keep them consistent. Next slice ([[012.plan]]): `page_service`.
