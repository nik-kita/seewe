---
kind: log
date: 2026-06-13
---

# Slice 7 cutover — code parts done (mount swap + nik-rename page cascade)

Two of slice 7's four parts (the code ones) landed:

1. **Mount swap** (`api/mod.ts`): `app.route("/", spa_subserver)` ->
   `page_subserver`. Public serving now reads `_dev_page` through the
   serving_machine. `spa_subserver`/`md-cv_service` left in tree (legacy, dead
   on the public path; removed in a later cleanup, not this slice).

2. **nik-rename page cascade** (`page_service.cascade_user_rename` +
   `users_service.update_nik`): the old `update_nik` cascade rewrote
   `_dev_md_cv` keys; replaced with `page_service.cascade_user_rename(user,
   nik)`. It rebuilds, for every page the user owns:
   - default page (routed by `default_by_user_id`): `default_by_username` =
     `normalize_username(new_nik)`, `display_username` = new_nik.
   - named pages (iterated via `user_id` secondary index, filtered on
     `by_username_and_name`): per-record `by_username_and_name = [new_lc,
     <existing lc_name>]` with `mergeOptions.arrays:"replace"` (same slug-merge
     gotcha that bit slice 6 [[023.log]]), `display_username` = new_nik.
   - then `purge_user(user._id)`.
   No import cycle (page_cache has no imports; page_service doesn't touch
   users_service).

Verify: `deno check mod.ts` clean; `deno task test` 22/22; throwaway smoke
10/10 (default + 2 named pages: keys lowercased, names kept, no array-merge
corruption, reachable at new username / unreachable at old). Smoke removed.

`noUncheckedIndexedAccess` required `by_username_and_name![1]!` on the kept
name slot.

REMAINING in slice 7 (ops, not code): (3) post-pivot schema photo against
remote KV (needs ACCESS_TOKEN); (4) hand off to paused [[kv-restore-migrate]]
for the data move. Until the migration runs, `_dev_page` is empty in
prod/dev-with-old-data, so the new public path resolves nothing for existing
records — expected, the data move is the next step.
