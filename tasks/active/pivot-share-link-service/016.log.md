---
kind: log
date: 2026-06-13
---

# Slice 4a done: render machine (xstate)

User chose the real npm:xstate v5 (not a dependency-free encoding). Added
`xstate@^5` to api/deno.json and `api/spa_subserver/render_machine.ts`.

`render_machine` (xstate `setup`/`createMachine`):
- states `resolve` -> (`is_pdf` guard) `serve_artifact` else `render_md`;
  `serve_artifact` invokes a `load_pdf` actor (`fromPromise`) -> `blob_found`
  guard -> `artifact_ready`, else/onError -> `render_md` (degrade).
- `load_pdf` is injected via input (no db import) so the machine is pure/testable.
- machine `output` -> `{ mode: "artifact", pdf } | { mode: "site" }`.
- `decide_render(input)` runs the actor to a final state via `toPromise`.

Verified (deno eval): md->site, pdf+blob->artifact, pdf-no-blob->site(degrade),
pdf-load-error->site(degrade). `deno check` clean.

## Slice 4 remaining sub-steps

- 4b: USER `nik` lc-index + display split. Needed because the page hot path is
  case-insensitive but the user record's nik is still raw — `Alice`/`alice`
  could both register (collision) and the no-page `find_by_nik` fallbacks aren't
  case-insensitive. Add `nik_lc` (indexed) + keep raw `nik` display; uniqueness +
  reserved checks on lc; set both in add_nik/update_nik.
- 4c: `page_subserver` — routes `/id/:user_id`, `/:username/:name`, `/:username`,
  `*`->serve_static; resolve via lc page indices; canonical display redirect
  (302/307) when incoming != display; wire `decide_render`.
- 4d: CDN cache headers — `Deno-CDN-Cache-Control: public, s-maxage=${CACHE_S_MAXAGE
  ?? long default}` + `Deno-Cache-Tag: page-<id>,user-<id>` ([[011.decision]]).
  Mount swap + the invalidate-on-write helper are later slices (5/7).
