---
kind: log
date: 2026-06-13
---

# Slice 5 done: invalidate-on-write (CDN tag purge)

Added purge helpers to `page_cache.ts` and wired them into every `page_service`
mutation.

- `purge_tags(tags)` POSTs `{tags}` to `http://cache.localhost/invalidate/http`
  (Deploy-only, global purge in seconds). Best-effort: wrapped in try/catch so a
  failure (e.g. local dev where the host doesn't resolve) never breaks a write.
  `purge_page(id)` / `purge_user(id)`.
- Wiring (precise): content edits -> `purge_page` (save-named, update, set_pdf,
  remove_pdf); default-link changes -> `purge_user` (save_as_default,
  toggle_default — one call covers old+new default page + /username).

Tests: 4 hermetic (stub fetch) — purge_page/purge_user post the right tag to the
right endpoint, empty-list no-ops, fetch failure is swallowed. Integration check
(stub fetch + real page_service): all six mutations fire the expected tag.
`deno check` + `deno task test` (22) clean.

Deferred: the nik-rename cascade `purge_user` lives in users_service, which still
targets the OLD `_dev_md_cv`; it gets the page-domain cascade + purge at cutover
(slice 7) / when the page router lands (slice 6).

Next: slice 6 routers (`/v1/page`, rename mdcv router, wire page_service).
