---
kind: log
date: 2026-06-13
---

# Render machine tests + backend test tooling

The api had no Deno tests and no test task. Added both.

- `api/spa_subserver/render_machine.test.ts` — 4 `Deno.test`s over `decide_render`
  using a `load_pdf` spy: md->site (and asserts the blob store is NOT read on the
  md path), pdf+blob->artifact (bytes match, loaded once), pdf-no-blob->degrade,
  pdf-load-error->degrade. 4/4 pass.
- `api/deno.json`: `tasks.test = "deno test -A"`, pinned `@std/assert@^1`.
- `JUSTFILE`: `test` recipe (`cd api && deno task test`).

This completes slice 4a (render machine) with committed coverage. Remaining slice
4: 4b user nik lc/display split, 4c page_subserver, 4d CDN cache headers.
