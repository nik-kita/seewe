---
kind: log
date: 2026-06-13
---

# Slice 4b done: user nik lc-index + display split

The page hot path was already case-insensitive (lc page indices), but the user
`nik` was still raw-indexed -> `Alice`/`alice` could both register and the
no-page `find_by_nik` fallback wasn't case-insensitive. Fixed.

- `utils/normalize_username.ts` — the single `trim().toLowerCase()` used by the
  reserved check, the `nik_lc` key, and (later) link resolution, so they agree.
  `is_reserved_username` now uses it.
- `UserDto`: added `nik_lc` (lowercased, indexed); `nik` stays the raw display.
- `db.ts` `_dev_users`: added `nik_lc: "primary"` (kept `nik` indexed for the
  still-running old domain).
- `users_service`: `add_nik`/`update_nik` now check uniqueness on `nik_lc` and
  store both `nik` + `nik_lc`; added `find_by_nik_lc` (lowercases then matches).

Verified (smoke, cleaned up): raw+lc stored; `find_by_nik_lc` case-insensitive;
`Alice` then `alice` rejected; reserved rejected; update rewrites lc and the old
lc stops resolving. `deno check mod.ts` clean.

Migration note (paused [[kv-restore-migrate]]): legacy users have no `nik_lc`, so
`find_by_nik_lc` won't find them until the migration backfills it. Old domain
still uses `find_by_nik` (raw), unaffected.

Next: 4c handler (`page_subserver`) — build ServeInput per route (using
`find_by_nik_lc` + the lc page indices) and turn ServeDecision into a Response;
then 4d CDN cache headers.
