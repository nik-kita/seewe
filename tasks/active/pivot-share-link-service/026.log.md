---
kind: log
date: 2026-06-14
---

# Produced + verified the new-schema fixture from live KV

Executed the produce step decided in [[025.decision]]. Token + DB id supplied by
user (DB `684989fd-...`).

What landed:
- `kv_dump.ts` against remote KV -> `dev/kv-dumps/kv-dump.remote.<stamp>.v8`,
  **181 raw entries** (all under `__kvdex__`; verbatim old schema). Gitignored.
- `db.ts` refactor: kvdex schema extracted into `export const make_db = (kv) =>
  kvdex(kv, {...})`; app still exports the default-kv `db = make_db(await
  Deno.openKv())`. No behavior change; 22/22 tests pass, `deno check mod.ts` clean.
- New `api/dev/kv_transform.ts`: dump -> throwaway local kv -> read via `make_db`
  -> write `_dev_page`/`_dev_page_pdf` via `add()` + backfill `nik_lc` via
  `updateByPrimaryIndex` -> raw-dump the 3 new-schema collections as the fixture.
  `transform_entry` seam in `kv_restore.ts` left identity (fixture is already
  target schema); added a pointer comment there.

Result (`kv-fixture.page.<stamp>.v8`, 187 raw entries, gitignored):
- **26 pages** from 26 cvs; **2 pdfs** reskeyed `mdcv_id`->`page_id` (177470-byte
  blob reassembled + re-chunked OK); **6/11 users** got `nik_lc`.
- collections: `_dev_page` 132, `_dev_page_pdf` 10, `_dev_users` 45 raw entries.

Verification (replayed fixture through kvdex, probed real lookups):
- case-insensitive user via `nik_lc` resolves (`KindKitchen`->kindkitchen).
- default links `/kindkitchen` (pdf), `/valentyn`, `/valentin`, `/nik` all OK —
  `Valentin`/`Valentyn` lowercase to distinct keys, no collision.
- named links `/kindkitchen/node-js`, `/nik/oboe`, `/Valentyn/umowa` (lc) resolve.
- 23 routable + 3 orphan (the legacy unindexed dupes, as intended); no `html`
  field leaked.

Derivation details in `kv_transform.ts` header + [[025.decision]]. CHANGELOG
updated under 2026-06-14.

Not done (still deferred): the APPLY/restore into remote KV — waits until the
rebrand (incl. UI) ships + deploys. Open sub-decision still open: generalize
`kind` beyond md/pdf.
