---
kind: log
date: 2026-06-13
---

# Status: active -> paused

Paused by the user with all required details captured: verbatim restore done;
`transform_entry` is the seam for the old->new mapping.

Forward note for whoever resumes (do not act now): the new version DROPS kvdex
(decided in [[pivot-share-link-service]]). So the eventual transform maps the
OLD kvdex layout (`__kvdex__/_dev_md_cv/...` primary+index entries from a dump)
into the NEW explicit raw-KV key layout — it rewrites keys between two different
storage schemes, not just a prefix swap. Hold until the explicit schema lands.
