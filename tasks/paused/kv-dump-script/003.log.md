---
kind: log
date: 2026-06-13
---

# Add schema "photo" + take the pre-pivot snapshot

Per the user's plan: remember the CURRENT schema now → do the API pivot →
complete it → diff old vs new schema to author the transform. The data dump
(kv_dump.ts) photos the data; this adds the matching photo of the schema.

Added `api/dev/schema_snapshot.ts`:
- Copies the stored-schema sources (`db.ts` kvdex collection/index config +
  `dto/user.dto.ts` + `dto/md-cv.dto.ts`) verbatim into
  `api/dev/schema-snapshots/<label>/`, plus a MANIFEST.
- COMMITTED (not gitignored, unlike kv-dumps/) — git is the memory of the old
  shape.
- Reproducible: after the rebrand, update `SCHEMA_SOURCES` to the renamed files
  and run with a `post-pivot` label; diff the two folders to drive
  `transform_entry` in [[kv-restore-migrate]].

Ran it: `dev/schema-snapshots/pre-pivot/` now holds the frozen pre-rebrand
schema (db.ts + the two DTOs + MANIFEST). `deno check` clean.

This was the missing piece of the "backup" — both data (kv_dump) and schema
(this) are now photographed before any pivot change. Remote data dump still the
user's run.
